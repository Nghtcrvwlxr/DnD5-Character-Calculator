export interface Race {
    index: string;
    name: string;
}

export interface Class {
    index: string;
    name: string;
}

export interface Background {
    index: string;
    name: string;
}

interface CardPageProps {
    fieldKey: string;
}